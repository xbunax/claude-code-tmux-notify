[project]
name = "agent-tmux-notify"
version = "0.1.0"
description = "Monitor Claude Code CLI in tmux panes"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["rich>=13.0", "pygments>=2.16"]

[project.scripts]
agent-tmux-notify = "claude_code_tmux_notify.cli:main"
